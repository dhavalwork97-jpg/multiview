# FGC OBS Bridge scripts

- `install-obs-bridge.cmd` — one-time Windows setup; saves configuration and enables automatic startup.
- `obs-bridge-status.cmd` — shows the Windows task state and bridge log location.
- `uninstall-obs-bridge.cmd` — removes automatic startup.

After installation, the bridge runs at Windows sign-in and uses dynamic tournament mode. No tournament ID is required.
