diff --git a/src/app/admin/tournaments/[tournamentId]/analytics/page.tsx b/src/app/admin/tournaments/[tournamentId]/analytics/page.tsx
index c03f3d4..8ef66dd 100644
--- a/src/app/admin/tournaments/[tournamentId]/analytics/page.tsx
+++ b/src/app/admin/tournaments/[tournamentId]/analytics/page.tsx
@@ -2,4 +2,4 @@ import Link from "next/link";
 import { notFound } from "next/navigation";
 import { db } from "@/lib/db";
 import { requireTournamentManage } from "@/lib/auth";
-export default async function AnalyticsPage({params}:{params:Promise<{tournamentId:string}>}){const {tournamentId}=await params;try{await requireTournamentManage(tournamentId);}catch{return notFound();}const t=await db.tournament.findUnique({where:{id:tournamentId},select:{name:true,matches:{select:{id:true,round:true,playerOne:{select:{gamertag:true}},playerTwo:{select:{gamertag:true}},status:true}},organizationId:true}});if(!t)notFound();const metrics=await db.eventDailyMetric.findMany({where:{tournamentId},orderBy:{dayKey:"asc"}});const views=metrics.reduce((a,m)=>a+m.views,0);const watch=metrics.reduce((a,m)=>a+m.watchSeconds,0);return <main className="min-h-screen bg-arena-950 px-6 py-8"><div className="mx-auto max-w-6xl"><Link href={`/admin/tournaments/${tournamentId}/control-room`} className="font-mono text-xs text-ink-faint">← Control room</Link><h1 className="mt-4 font-display text-3xl uppercase">{t.name} analytics</h1><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-card border border-arena-700 bg-arena-900 p-5"><p className="font-mono text-[10px] uppercase text-ink-faint">Views</p><p className="mt-2 text-3xl">{views}</p></div><div className="rounded-card border border-arena-700 bg-arena-900 p-5"><p className="font-mono text-[10px] uppercase text-ink-faint">Watch hours</p><p className="mt-2 text-3xl">{(watch/3600).toFixed(1)}</p></div><div className="rounded-card border border-arena-700 bg-arena-900 p-5"><p className="font-mono text-[10px] uppercase text-ink-faint">Matches</p><p className="mt-2 text-3xl">{t.matches.length}</p></div></div><div className="mt-8 rounded-card border border-arena-700 bg-arena-900 p-5"><h2 className="font-display text-xl uppercase">Match activity</h2><div className="mt-3 divide-y divide-arena-700">{t.matches.slice(0,30).map(m=><div key={m.id} className="flex justify-between gap-3 py-3 text-sm"><span>{m.playerOne.gamertag} vs {m.playerTwo.gamertag}</span><span className="font-mono text-[10px] text-ink-faint">{m.round} · {m.status}</span></div>)}</div></div></div></main>}
+export default async function AnalyticsPage({params}:{params:Promise<{tournamentId:string}>}){const {tournamentId}=await params;try{await requireTournamentManage(tournamentId);}catch{return notFound();}const t=await db.tournament.findUnique({where:{id:tournamentId},select:{name:true,matches:{select:{id:true,round:true,playerOne:{select:{gamertag:true}},playerTwo:{select:{gamertag:true}},status:true}},organizationId:true}});if(!t)notFound();const metrics=await db.eventDailyMetric.findMany({where:{tournamentId},orderBy:{dayKey:"asc"}});const views=metrics.reduce((a,m)=>a+m.views,0);const watch=metrics.reduce((a,m)=>a+m.watchSeconds,0);return <main className="min-h-screen bg-arena-950 px-6 py-8"><div className="mx-auto max-w-6xl"><Link href={`/admin/tournaments/${tournamentId}/control-room`} className="font-mono text-xs text-ink-faint">← Control room</Link><h1 className="mt-4 font-display text-3xl uppercase">{t.name} analytics</h1><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-card border border-arena-700 bg-arena-900 p-5"><p className="font-mono text-[10px] uppercase text-ink-faint">Views</p><p className="mt-2 text-3xl">{views}</p></div><div className="rounded-card border border-arena-700 bg-arena-900 p-5"><p className="font-mono text-[10px] uppercase text-ink-faint">Watch hours</p><p className="mt-2 text-3xl">{(watch/3600).toFixed(1)}</p></div><div className="rounded-card border border-arena-700 bg-arena-900 p-5"><p className="font-mono text-[10px] uppercase text-ink-faint">Matches</p><p className="mt-2 text-3xl">{t.matches.length}</p></div></div><div className="mt-8 rounded-card border border-arena-700 bg-arena-900 p-5"><h2 className="font-display text-xl uppercase">Match activity</h2><div className="mt-3 divide-y divide-arena-700">{t.matches.slice(0,30).map(m=><div key={m.id} className="flex justify-between gap-3 py-3 text-sm"><span>{m.playerOne?.gamertag ?? "Side A"} vs {m.playerTwo?.gamertag ?? "Side B"}</span><span className="font-mono text-[10px] text-ink-faint">{m.round} · {m.status}</span></div>)}</div></div></div></main>}
diff --git a/src/app/api/brackets/[bracketId]/route.ts b/src/app/api/brackets/[bracketId]/route.ts
index eb64b1e..004f2b9 100644
--- a/src/app/api/brackets/[bracketId]/route.ts
+++ b/src/app/api/brackets/[bracketId]/route.ts
@@ -59,11 +59,11 @@ export async function GET(_req: Request, { params }: { params: Promise<{ bracket
   const gamertagByPlayerId: Record<string, string> = {};
   for (const m of bracket.matches) {
     if (m.status === "LIVE") {
-      liveMatchByPlayerId[m.playerOneId] = m.id;
-      liveMatchByPlayerId[m.playerTwoId] = m.id;
+      if (m.playerOneId) liveMatchByPlayerId[m.playerOneId] = m.id;
+      if (m.playerTwoId) liveMatchByPlayerId[m.playerTwoId] = m.id;
     }
-    gamertagByPlayerId[m.playerOneId] = m.playerOne.gamertag;
-    gamertagByPlayerId[m.playerTwoId] = m.playerTwo.gamertag;
+    if (m.playerOneId && m.playerOne) gamertagByPlayerId[m.playerOneId] = m.playerOne.gamertag;
+    if (m.playerTwoId && m.playerTwo) gamertagByPlayerId[m.playerTwoId] = m.playerTwo.gamertag;
   }
 
   return NextResponse.json({
diff --git a/src/app/api/matches/[matchId]/route.ts b/src/app/api/matches/[matchId]/route.ts
index 2431f44..e96a055 100644
--- a/src/app/api/matches/[matchId]/route.ts
+++ b/src/app/api/matches/[matchId]/route.ts
@@ -302,7 +302,7 @@ export async function PATCH(req: Request, { params }: { params: Promise<{ matchI
   // progression — see src/lib/bracket-progression.ts for why this is the
   // single write path for that (score-keeper/organizer PATCH is already
   // the single write path for match state generally).
-  if (updated.status === "COMPLETED" && updated.winnerId && updated.bracketId) {
+  if (updated.status === "COMPLETED" && updated.winnerId && updated.bracketId && existing.playerOneId && existing.playerTwoId) {
     const advanced = await db.$transaction((tx) => advanceBracket(tx, {
       ...updated,
       playerOneId: existing.playerOneId,
diff --git a/src/lib/match-engine/adapters.ts b/src/lib/match-engine/adapters.ts
index 9e8bd5d..702e6ef 100644
--- a/src/lib/match-engine/adapters.ts
+++ b/src/lib/match-engine/adapters.ts
@@ -46,7 +46,7 @@ export const battleRoyaleAdapter: ScoringAdapter = {
     return allowed.includes(metric);
   },
   score(side, rules) {
-    const weights = { placement: 0, kills: 1, points: 1, ...(rules.weights ?? {}) };
+    const weights: Record<string, number> = { placement: 0, kills: 1, points: 1, ...(rules.weights ?? {}) };
     return side.events.reduce((total, event) => total + event.value * (weights[event.metric] ?? 0), 0);
   },
   resolve(a, b, rules) {
