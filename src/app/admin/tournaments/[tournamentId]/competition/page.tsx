import Link from "next/link";
import { notFound } from "next/navigation";
import { db } from "@/lib/db";
import { requireTournamentManage } from "@/lib/auth";
import { getCompetitionPreset } from "@/lib/competition-engine";
import { MultiStageGenerator } from "@/components/admin/MultiStageGenerator";
export default async function CompetitionRulesPage({params}:{params:Promise<{tournamentId:string}>}){const {tournamentId}=await params;try{await requireTournamentManage(tournamentId);}catch{return notFound();}const t=await db.tournament.findUnique({where:{id:tournamentId},select:{id:true,name:true,sport:true,game:true,format:true,bestOf:true,participantMode:true,scoringMode:true,competitionType:true,competitionRules:true,stages:{orderBy:{orderIndex:"asc"},include:{matches:{select:{id:true,status:true}}}}}});if(!t)notFound();const preset=getCompetitionPreset(t.sport);return <main className="min-h-screen bg-arena-950 px-6 py-8"><div className="mx-auto max-w-6xl"><Link href={`/admin/tournaments/${t.id}`} className="font-mono text-xs text-ink-faint">← Tournament admin</Link><h1 className="mt-3 font-display text-3xl uppercase">Competition rules</h1><p className="mt-1 text-sm text-ink-faint">{t.name} · generic match engine configuration</p><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Card label="Sport" value={t.sport}/><Card label="Game / discipline" value={t.game}/><Card label="Participant mode" value={t.participantMode}/><Card label="Scoring" value={t.scoringMode}/><Card label="Format" value={t.format}/><Card label="Best of" value={String(t.bestOf)}/><Card label="Competition type" value={t.competitionType}/><Card label="Engine" value="generic-match-engine v1"/></div><section className="mt-6 rounded-card border border-arena-700 bg-arena-900 p-5"><h2 className="font-display text-xl uppercase">Competition stages</h2><p className="mt-1 text-sm text-ink-faint">Persistent V28 phases used by generic advancement.</p><div className="mt-4 grid gap-3 md:grid-cols-2">{t.stages.map((stage,index)=><div key={stage.id} className="rounded-card border border-arena-700 bg-arena-950 p-4"><div className="flex items-center justify-between"><span className="font-mono text-[10px] uppercase text-ink-faint">Stage {index+1}</span><span className="font-mono text-[10px] uppercase text-ink-faint">{stage.status}</span></div><h3 className="mt-2 font-display uppercase">{stage.name}</h3><p className="mt-1 text-xs text-ink-faint">{stage.kind} · {stage.matches.length} matches</p></div>)}</div></section><MultiStageGenerator tournamentId={t.id} disabled={t.stages.length > 0} /><section className="mt-6 rounded-card border border-arena-700 bg-arena-900 p-5"><h2 className="font-display text-xl uppercase">Preset rules</h2><p className="mt-1 text-sm text-ink-faint">{preset.label}</p><pre className="mt-4 overflow-x-auto rounded-card bg-arena-950 p-4 text-xs text-ink-muted">{JSON.stringify(preset.rules,null,2)}</pre></section><section className="mt-6 rounded-card border border-arena-700 bg-arena-900 p-5"><h2 className="font-display text-xl uppercase">Tournament rules snapshot</h2><pre className="mt-4 overflow-x-auto rounded-card bg-arena-950 p-4 text-xs text-ink-muted">{JSON.stringify(t.competitionRules ?? {},null,2)}</pre></section></div></main>}
function Card({label,value}:{label:string;value:string}){return <div className="rounded-card border border-arena-700 bg-arena-900 p-4"><p className="font-mono text-[10px] uppercase text-ink-faint">{label}</p><p className="mt-2 font-semibold">{value}</p></div>}
