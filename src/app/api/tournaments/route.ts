// Stub — full tournament CRUD (create/list/update, bracket import trigger)
// lands in Phase 2 alongside the organizer's tournament-creation UI.
// Left as a placeholder so the route tree in the README matches disk now.
export {};
