# Moderator Center

The Organizer Moderator Center is intentionally backed by the existing `TournamentIncident` workflow.

It provides organization-scoped visibility into incident severity, status, tournament context, creation/resolution timestamps, and a direct handoff to station operations.

It does not expose mute, ban, report, or chat-deletion controls because those actions are not currently backed by a verified moderation API/model in this repository.
