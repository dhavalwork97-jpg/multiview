"use client";
import { useEffect,useState } from "react";
export function BrandingForm(){const [data,setData]=useState<any>(null);const [busy,setBusy]=useState(false);const [msg,setMsg]=useState("");useEffect(()=>{fetch("/api/organizations/current/branding").then(r=>r.json()).then(j=>setData(j.organization)).catch(()=>{});},[]);if(!data)return <div className="rounded-card border border-arena-700 p-5">Loading organization settings…</div>;async function save(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");const r=await fetch("/api/organizations/current/branding",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const j=await r.json();setBusy(false);setMsg(r.ok?"Saved":"Could not save");if(r.ok)setData(j.organization);}return <form onSubmit={save} className="space-y-5 rounded-card border border-arena-700 bg-arena-900 p-5"><h2 className="font-display text-xl uppercase">Branding</h2><div className="grid gap-4 md:grid-cols-2">{[["name","Organization name"],["tagline","Tagline"],["brandLogoUrl","Logo URL"],["customDomain","Custom domain"]].map(([k,l])=><label key={k}><span className="mb-1 block font-mono text-[10px] uppercase text-ink-faint">{l}</span><input value={data[k]??""} onChange={e=>setData({...data,[k]:e.target.value||null})} className="w-full rounded-card border border-arena-600 bg-arena-950 px-3 py-2 text-sm" /></label>)}<label><span className="mb-1 block font-mono text-[10px] uppercase text-ink-faint">Primary color</span><input type="color" value={data.brandPrimaryColor??"#7cf7c5"} onChange={e=>setData({...data,brandPrimaryColor:e.target.value})} className="h-10 w-full rounded-card bg-arena-950" /></label><label><span className="mb-1 block font-mono text-[10px] uppercase text-ink-faint">Accent color</span><input type="color" value={data.brandAccentColor??"#7c9cff"} onChange={e=>setData({...data,brandAccentColor:e.target.value})} className="h-10 w-full rounded-card bg-arena-950" /></label></div><button disabled={busy} className="rounded-card bg-signal-live px-4 py-2 font-mono text-xs uppercase text-arena-950">{busy?"Saving…":"Save branding"}</button>{msg&&<span className="ml-3 text-xs text-ink-faint">{msg}</span>}</form>}
